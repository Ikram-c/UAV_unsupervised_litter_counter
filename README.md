# UAV_unsupervised_litter_counter
An alternative (to mainstream UK OCPP approach which I spent 2 years spearheading on the backends) method to quality controlling any image datasets (both training and inference) packaged in a user friendly and lightweight repo, rapid enough to process tera bytes in seconds, efficient enough to run on a single core thinkpad
